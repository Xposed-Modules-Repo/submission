name: "Submission"
description: "create repositories in request of issues"
inputs:
  github-token:
    description: "Github token"
    required: true
runs:
  using: "node20"
  main: "index.js"
  pre: 'yarn'
